export const metadata = {
  title: "v1.0.1",
  description: "Connection close on idle handling",
};

export default function RootLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
